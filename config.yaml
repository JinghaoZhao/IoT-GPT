# Sensor configuration
sensors:
  # Temperature sensor
  - type: temperature # Sensor type (e.g., temperature, humidity, pressure, etc.)
    model: DHT22 # Sensor model
    pin: 4 # Pin number on the board

  # Humidity sensor
  - type: humidity # Sensor type (e.g., temperature, humidity, pressure, etc.)
    model: DHT22 # Sensor model
    pin: 4 # Pin number on the board

# Network configuration
networks:
  # Cellular network
  - category: cellular # Network category (e.g., cellular, wifi)
    type: Cat-M1 # Network type (e.g., NB-IoT, Cat-M, LTE, 5G, etc.)
    apn: your_apn # Access Point Name for cellular network

  # WiFi network
  - category: wifi # Network category (e.g., cellular, wifi)
    ssid: your_ssid # WiFi SSID
    password: your_wifi_password # WiFi password

# Traffic interval configuration
traffic_interval: 60 # Value of seconds between two transmissions

# Protocol configuration
protocol:
  type: MQTT # IoT protocol (e.g., CoAP or MQTT)
  broker: localhost # Broker address
  # Broker port (default: 1883 for MQTT not encrypted, 8883 for MQTT encrypted,
  # 5683 for CoAP not encrypted, 5684 for CoAP encrypted)
  port: 8883
  username: '' # Broker username (optional)
  password: '' # Broker password (mandatory if CoAP with DTLS is used)
  topic: test/topic # Topic for publishing data (mandatory)
  security:
    encryption: TLS # Encryption type: TLS support for MQTT, DTLS support for CoAP
    ca_certificate_path: './certs/ca.crt' # CA certificate path (mandatory for TLS)
    client_certificate_path: './certs/client.crt' # Client certificate path (mandatory for TLS)
    client_key_path: './certs/client.key' # Client key path (mandatory for TLS)
    psk: '' # Pre-shared key (mandatory for DTLS)